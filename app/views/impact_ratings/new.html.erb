<% content_for :title, 'New impact rating' %>

<div class="outer-container md:w-4/5 my-5">
  <div class="mb-5">
    <%= link_to '‹ Back', impactable_path(@impactable), class: 'link' %>
  </div>

  <section class="p-5 bg-white rounded-lg shadow mb-6">
    <h1 class="text-xl text-blue-800 leading-tight">New impact rating</h1>
    <hr class="mt-2 mb-6 border-gray-300">
    <%= render 'form', model: @impact_rating, url: impactable_impact_ratings_path(@impactable) %>
  </section>
</div>
