<div class="bg-purple-800">
  <section class="outer-container py-4 text-white">
    <div class="flex justify-between mb-2">
      <div class="mr-4">
        <p class="inline-flex text-white text-xs font-semibold tracking-widest uppercase bg-purple-900 rounded px-2 py-1 mb-1"><%= @goal.status.humanize %></p>
        <h1 class="text-white text-xl font-semibold"><%= @goal.title %></h1>
      </div>
      <%= link_to 'Edit', edit_goal_path(@goal), class: 'link text-gray-200' %>
    </div>

    <div class="mb-2">
      <%- @goal.tags.each do |tag| %>
        <%= link_to goals_path(q: { tags_id_in: tag.id }), class: 'inline-block mr-2 text-sm hover:underline' do %>
          #<%= tag.title.parameterize %>
        <% end %>
      <% end %>
    </div>

    <div class="flex flex-wrap items-center text-white text-sm">

      <%= link_to @goal do %>
        <div class="px-2 py-1 mr-1 mb-1 bg-gray-800 bg-opacity-25 hover:bg-gray-800 rounded">
          Overview
        </div>
      <% end %>

      <%= link_to goal_memberships_path(@goal) do %>
        <div class="px-2 py-1 mr-1 mb-1 bg-gray-800 bg-opacity-25 hover:bg-gray-800 rounded">
          Team (<%= @goal.memberships.size %>)
        </div>
      <% end %>
    </div>
  </section>
</div>
