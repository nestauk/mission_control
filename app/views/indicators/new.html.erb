<% content_for :title, 'New indicator - #{@targetable.title}' %>

<div class="mx-auto max-w-screen-md">
  <div class="m-5">
    <%= link_to '‹ Back', targetable_path(@targetable), class: 'link' %>
  </div>

  <section class="m-5 p-5 bg-white rounded-lg shadow">
    <h1 class="font-semibold text-xl text-blue-800 leading-tight">
      New indicator
    </h1>
    <hr class="mt-2 mb-6 border-gray-300">
    <%= render 'form', model: @indicator, url: targetable_indicators_path(@targetable) %>
  </section>
</div>
