<% content_for :title, 'Add member' %>

<div class="outer-container md:w-4/5 my-5">
  <div class="mb-5">
    <%= link_to '‹ Back', memberable_memberships_path(@memberable), class: 'link' %>
  </div>

  <section class="p-5 bg-white rounded-lg shadow mb-6">
    <h1 class="text-xl text-blue-800 leading-tight">Add membership</h1>
    <hr class="mt-2 mb-6 border-gray-300">
    <%= render 'form', model: @membership, url: memberable_memberships_path(@memberable) %>
  </section>
</div>
