project_1:
  title: Project 1
  objective: Project objective
  start_date: <%= Date.today %>
  end_date: <%= 4.weeks.since %>
  public_uid: <%= SecureRandom.random_number(1_000_000) %> # generate public_uid
